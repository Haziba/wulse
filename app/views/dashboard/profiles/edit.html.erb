<% content_for :title, "Edit Profile" %>

<!-- Main Content -->
<div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
  <!-- Page Header -->
  <div class="mb-8">
    <div>
      <h2 class="font-serif text-3xl font-bold text-gray-900 mb-2">Edit Profile</h2>
      <p class="text-gray-600">Update your personal information and account settings</p>
    </div>
  </div>

  <!-- Profile Form -->
  <div id="profile_form">
    <%= render "form", staff: @staff %>
  </div>
</div>
