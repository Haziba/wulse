<!DOCTYPE html>
<html lang="en">
  <head>
    <title><%= content_for(:title) || "Dashboard - #{Current.institution.name.titleize}" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= javascript_importmap_tags %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Google Fonts %>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;500;600;700&family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">

    <%# Font Awesome %>
    <script> window.FontAwesomeConfig = { autoReplaceSvg: 'nest'};</script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/js/all.min.js" crossorigin="anonymous" referrerpolicy="no-referrer"></script>

    <%# Tailwind CSS %>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
      ::-webkit-scrollbar { display: none; }
    </style>
    <script>
      <%
        branding_colour = Current.institution&.branding_colour || "#8B4513"
        color_shades = generate_color_shades(branding_colour)
      %>
      tailwind.config = {
        theme: {
          extend: {
            colors: {
              brand: {
                50: '<%= color_shades[50] %>',
                100: '<%= color_shades[100] %>',
                200: '<%= color_shades[200] %>',
                300: '<%= color_shades[300] %>',
                400: '<%= color_shades[400] %>',
                500: '<%= color_shades[500] %>',
                600: '<%= color_shades[600] %>',
                700: '<%= color_shades[700] %>',
                800: '<%= color_shades[800] %>',
                900: '<%= color_shades[900] %>',
              }
            },
            fontFamily: {
              'serif': ['Playfair Display', 'serif'],
              'sans': ['Inter', 'sans-serif'],
            }
          }
        }
      }
    </script>
  </head>

  <body class="font-sans bg-gray-50">
    <%= render "shared/demo_ribbon" if Current.institution&.demo? %>

    <!-- Skip Link -->
    <a href="#main-content" class="sr-only focus:not-sr-only focus:absolute focus:top-4 focus:left-4 focus:z-[100] focus:bg-brand-500 focus:text-white focus:px-4 focus:py-2 focus:rounded-lg">
      Skip to main content
    </a>

    <!-- Brand Strip -->
    <div class="h-2 bg-gradient-to-r from-brand-900 via-brand-700 to-brand-900" aria-hidden="true"></div>

    <%= render 'shared/dashboard_header' %>

    <!-- Flash Messages -->
    <div id="toast-container-target" class="fixed bottom-8 left-1/2 transform -translate-x-1/2 z-50 w-full max-w-md px-4">
      <%= render 'shared/toast_flash' %>
    </div>

    <!-- Main Content -->
    <main id="main-content" class="min-h-screen" role="main">
      <%= yield %>
    </main>

    <!-- Footer -->
    <footer class="bg-gradient-to-r from-brand-900 via-brand-800 to-brand-900 text-white mt-20">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-6">
        <div class="flex flex-wrap justify-center items-center gap-3 text-xs text-brand-200">
          <span>© <%= Time.current.year %> <%= Current.institution.name.titleize %></span>
          <span aria-hidden="true">·</span>
          <%= link_to "Terms of Service", terms_path, class: "hover:text-white transition-colors underline" %>
          <span aria-hidden="true">·</span>
          <%= link_to "Privacy Policy", privacy_path, class: "hover:text-white transition-colors underline" %>
        </div>
      </div>
    </footer>
  </body>
</html>
