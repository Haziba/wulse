<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "University Research Library" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= javascript_importmap_tags %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="apple-touch-icon" href="/icon.png">
    <%# Google Fonts %>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;500;600;700&family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">

    <%# Font Awesome %>
    <script> window.FontAwesomeConfig = { autoReplaceSvg: 'nest'};</script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/js/all.min.js" crossorigin="anonymous" referrerpolicy="no-referrer"></script>

    <%# Tailwind CSS %>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
      <%
        branding_colour = Current.institution&.branding_colour || "#e0486d"
        color_shades = generate_color_shades(branding_colour)
      %>
      tailwind.config = {
        theme: {
          extend: {
            colors: {
              brand: {
                50: '<%= color_shades[50] %>',
                100: '<%= color_shades[100] %>',
                200: '<%= color_shades[200] %>',
                300: '<%= color_shades[300] %>',
                400: '<%= color_shades[400] %>',
                500: '<%= color_shades[500] %>',
                600: '<%= color_shades[600] %>',
                700: '<%= color_shades[700] %>',
                800: '<%= color_shades[800] %>',
                900: '<%= color_shades[900] %>',
              }
            },
            fontFamily: {
              'serif': ['Playfair Display', 'serif'],
              'sans': ['Inter', 'sans-serif'],
            }
          }
        }
      }
    </script>
  </head>

  <body class="bg-white font-sans">
    <!-- Brand Strip -->
    <div class="h-2 bg-gradient-to-r from-brand-900 via-brand-700 to-brand-900"></div>

    <!-- Header -->
    <header class="bg-white border-b border-gray-200 sticky top-0 z-50 shadow-sm">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-4">
        <div class="flex items-center justify-between gap-8">
          <!-- Logo -->
          <%= link_to "/", class: "flex items-center gap-3 flex-shrink-0" do %>
            <% if Current.institution.logo.attached? %>
              <%= image_tag Current.institution.logo, alt: "#{Current.institution.name.titleize} Logo", class: "h-10 w-10" %>
            <% end %>
            <span class="font-serif text-xl font-bold text-brand-900"><%= Current.institution.name.titleize %></span>
          <% end %>

          <!-- Search Bar (Desktop) -->
          <form action="/library" method="get" class="hidden md:flex flex-1 max-w-xl">
            <input type="text" name="q" placeholder="Search..." class="flex-1 px-4 py-2 border border-gray-300 rounded-l-lg focus:outline-none focus:ring-2 focus:ring-brand-600 focus:border-transparent">
            <button type="submit" class="px-4 py-2 bg-brand-500 text-white rounded-r-lg hover:bg-brand-600 transition-colors">
              <i class="fas fa-search"></i>
            </button>
          </form>

          <!-- Navigation -->
          <nav class="flex items-center gap-4">
            <%= link_to new_session_path, class: "p-2 bg-brand-500 text-white rounded-lg hover:bg-brand-600 transition-colors" do %>
              <i class="fas fa-user"></i>
            <% end %>
          </nav>
        </div>

        <!-- Mobile Search -->
        <form action="/library" method="get" class="md:hidden mt-4 flex">
          <input type="text" name="q" placeholder="Search..." class="flex-1 px-4 py-2 border border-gray-300 rounded-l-lg focus:outline-none focus:ring-2 focus:ring-brand-600">
          <button type="submit" class="px-4 py-2 bg-brand-500 text-white rounded-r-lg hover:bg-brand-600 transition-colors">
            <i class="fas fa-search"></i>
          </button>
        </form>
      </div>
    </header>

    <!-- Flash Messages -->
    <div id="toast-container-target" class="fixed bottom-8 left-1/2 transform -translate-x-1/2 z-50 w-full max-w-md px-4">
      <%= render 'shared/toast_flash' %>
    </div>

    <!-- Main Content -->
    <main class="bg-white">
      <%= yield %>
    </main>

    <!-- Footer -->
    <footer class="bg-gradient-to-r from-brand-900 via-brand-800 to-brand-900 text-white mt-20">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
        <div class="text-center">
          <p class="text-brand-200 text-sm">© <%= Time.current.year %> <%= Current.institution.name.titleize %> Research Library. All rights reserved.</p>
        </div>
      </div>
    </footer>
  </body>
</html>
